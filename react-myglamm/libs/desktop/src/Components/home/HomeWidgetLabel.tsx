import React from "react";

const HomeWidgetLabel = ({ title }: any) => (
  <h3 className="mx-auto mb-6 mt-0 font-semibold w-max uppercase px-1.5 bg-title-pattern text-2xl">{title}</h3>
);

export default HomeWidgetLabel;
