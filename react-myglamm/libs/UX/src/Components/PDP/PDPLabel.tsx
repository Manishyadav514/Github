import * as React from "react";

const PDPLabel = ({ label }: any) => (
  <div className="flex justify-start px-4 mb-4">
    <span className="text-sm text-black font-bold uppercase inline-block bg-underline">{label}</span>
  </div>
);

export default PDPLabel;
